<% if comment.id %>
  <%= link_to comment.user.name, comment.user %>:
  <%= comment.content %></br>
  <%= time_ago_in_words(comment.created_at) %>
  <%= link_to "delete", comment_path(comment), method: :delete, data: {confirm: "You sure?"} if current_user? comment.user%>
  </br></br>
<% end %>
